import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Little_lemon',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
